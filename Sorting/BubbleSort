def BubbleSort(arr):
	print(arr)
	count = 0;
	for i in range(len(arr),0,-1):
		count = 0;
		for j in range(0,i-1):
			if arr[j]>arr[j+1]:
				arr[j],arr[j+1]=arr[j+1],arr[j]
				count = 1;
		if count==0:
			return arr;
