def SelectionSort(arr):
	print(arr)
	for i in range(len(arr)):
		minm = i
		for j in range(i+1,len(arr)):
			if arr[j]<arr[minm]:
				minm = j
				
		if i != minm:
			arr[i],arr[minm] = arr[minm],arr[i]
	print(arr)
